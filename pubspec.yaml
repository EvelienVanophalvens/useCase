name: demo
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.6
  get_it: ^8.0.2
  http: ^1.2.2
  injectable: ^2.5.0
  mockito: ^5.4.4
  mocktail: ^1.0.4
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  test: ^1.25.7
  bloc_test: ^9.1.7
  injectable_generator: ^2.6.2
  build_runner: ^2.4.13
  

flutter:
  uses-material-design: true
